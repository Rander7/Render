#include<stdio.h>
int main()
{
	int i, j, n, s1, sum;
	scanf("%d", &n);
	s1 = 0;
	sum = 0;
	for(i=1;i<=n;i++)
	{
		for (j = 1; j <= i; j++)
		{
			s1 += j;
		}

		
	}
	sum += s1;
	printf("%d", sum);
	return 0;
}
