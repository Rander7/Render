#include<stdio.h>
int main()
{
	int a, b, c;
	float s;//一定要用float，不然会乱码
	for (a = 0; a < 9; a++)
	{
		for (b = 0; b < 12; b++)
		{
			c = 36 - a - b;

			s = 4 * a + 3 * b + c/ 2.0;

			if (s == 36)
				printf("%d%d%d", a, b, c);
		}
	}
	return 0;
}
